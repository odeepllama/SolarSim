# Single UF2 Builder Dependencies
# Install with: pip install -r requirements-build.txt

# LittleFS filesystem support (REQUIRED)
littlefs-python>=0.5.0

# Standard library modules used:
# - urllib (built-in)
# - ssl (built-in)
# - struct (built-in)
# - argparse (built-in)
# - pathlib (built-in)
